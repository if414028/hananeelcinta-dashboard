<x-layouts.admin title="Role & Permission">
    <div class="mb-8"><p class="text-sm font-bold uppercase tracking-[.04em] text-slate"><span class="text-signal-light">•</span> Akses</p><h1 class="mt-2 text-4xl">Role & Permission</h1><p class="mt-3 text-slate">Super Admin selalu memiliki seluruh akses dan tidak dapat dibatasi.</p></div>
    <div class="grid gap-6 md:grid-cols-2">@foreach($roles as $role)<x-card><div class="flex items-start justify-between"><div><h2 class="text-2xl">{{ $role->name }}</h2><p class="mt-2 text-slate">{{ $role->permissions_count }} permission · {{ $role->users_count }} admin</p></div>@if($role->name!=='Super Admin')<a href="{{ route('admin.roles.edit',$role) }}" class="rounded-[20px] border border-primary px-5 py-2 text-primary">Atur</a>@else<x-badge tone="success">Akses penuh</x-badge>@endif</div></x-card>@endforeach</div>
</x-layouts.admin>
