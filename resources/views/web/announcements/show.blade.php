<x-layouts.public :title="$announcement->title" :description="$announcement->excerpt?:str(strip_tags($announcement->description))->limit(160)" :image="$announcement->image_url" type="article"><article class="page-container pb-24 pt-40 lg:pt-48"><a href="{{ route('announcements.index') }}" class="text-sm underline">← Semua pengumuman</a><header class="mt-8 max-w-5xl"><p class="text-sm font-bold uppercase tracking-[.04em] text-slate"><span class="text-signal-light">•</span> {{ $announcement->published_at->format('d M Y') }}</p><h1 class="mt-5 text-5xl leading-none md:text-7xl">{{ $announcement->title }}</h1><p class="mt-7 max-w-3xl text-xl leading-8 text-slate">{{ $announcement->excerpt }}</p></header><img src="{{ $announcement->image_url }}" alt="{{ $announcement->title }}" class="mt-12 aspect-[16/8] w-full rounded-[40px] bg-ink/5 object-cover"><div class="mt-12 grid gap-10 lg:grid-cols-[1fr_18rem]"><div class="prose-public max-w-3xl whitespace-pre-line">{{ $announcement->description }}</div><aside><x-card title="Informasi">@if($announcement->contact_person_name)<p class="text-sm text-slate">Contact person</p><p class="mt-1">{{ $announcement->contact_person_name }}</p>@endif @if($announcement->contact_person_phone)<a class="mt-5 block underline" href="tel:{{ $announcement->contact_person_phone }}">{{ $announcement->contact_person_phone }}</a>@endif @if($announcement->information_url)<a class="mt-5 block underline" href="{{ $announcement->information_url }}" target="_blank" rel="noopener">Informasi selengkapnya ↗</a>@endif</x-card></aside></div></article>@if($related->isNotEmpty())<section class="bg-lifted py-20"><div class="page-container"><h2 class="mb-10 text-4xl">Pengumuman lainnya</h2><div class="grid gap-12 md:grid-cols-3">@foreach($related as $announcement)@include('web.partials.announcement-card')@endforeach</div></div></section>@endif</x-layouts.public>
