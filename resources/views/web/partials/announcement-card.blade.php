<article class="group"><a href="{{ route('announcements.show',$announcement) }}" class="block"><div class="aspect-square overflow-hidden rounded-full bg-ink/5"><img src="{{ $announcement->image_url }}" alt="{{ $announcement->title }}" class="h-full w-full object-cover transition duration-500 group-hover:scale-105" loading="lazy"></div><p class="mt-6 text-xs font-bold uppercase tracking-[.04em] text-slate"><span class="text-signal-light">•</span> Pengumuman · {{ $announcement->published_at->format('d M Y') }}</p><h3 class="mt-3 text-2xl">{{ $announcement->title }}</h3><p class="mt-3 line-clamp-3 text-slate">{{ $announcement->excerpt ?: str(strip_tags($announcement->description))->limit(140) }}</p></a></article>
