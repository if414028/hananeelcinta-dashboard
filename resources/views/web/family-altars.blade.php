<x-layouts.public title="Mezbah Keluarga" description="Temukan jadwal dan lokasi Mezbah Keluarga JKI Hananeel Cinta."><section class="page-container pb-16 pt-40 lg:pt-48"><p class="text-sm font-bold uppercase tracking-[.04em] text-slate"><span class="text-signal-light">•</span> Komunitas</p><h1 class="mt-6 text-5xl md:text-7xl">Mezbah Keluarga</h1><p class="mt-7 max-w-2xl text-xl leading-8 text-slate">Tempat untuk bertumbuh, berbagi kehidupan, dan saling menguatkan dalam komunitas yang lebih dekat.</p></section><section class="bg-lifted py-20"><div class="page-container"><form method="get" class="mb-12 grid gap-4 rounded-[40px] bg-white p-6 md:grid-cols-[1fr_1fr_auto]"><x-input name="search" label="Cari lokasi atau PIC" :value="request('search')"/><x-select name="day" label="Hari"><option value="">Semua hari</option>@foreach($days as $value=>$label)<option value="{{ $value }}" @selected(request('day')===$value)>{{ $label }}</option>@endforeach</x-select><div class="flex items-end"><x-button type="submit">Terapkan</x-button></div></form><div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">@forelse($items as $altar)<article class="rounded-[40px] bg-white p-7"><x-badge>{{ $altar->day_of_week->label() }}</x-badge><h2 class="mt-5 text-2xl">{{ $altar->name }}</h2><p class="mt-2 text-slate">{{ $altar->start_time }}@if($altar->end_time)–{{ $altar->end_time }}@endif</p><div class="mt-5 space-y-2 text-sm"><p><strong>PIC:</strong> {{ $altar->pic_name?:'-' }}</p><p><strong>Lokasi:</strong> {{ $altar->location_name?:$altar->address }}</p>@if($altar->description)<p class="pt-2 text-slate">{{ $altar->description }}</p>@endif</div><div class="mt-7 flex flex-wrap gap-3">@if($altar->whatsapp_url)<a href="{{ $altar->whatsapp_url }}" target="_blank" rel="noopener" class="rounded-[20px] bg-primary px-5 py-2 text-canvas">WhatsApp</a>@endif @if($altar->map_url)<a href="{{ $altar->map_url }}" target="_blank" rel="noopener" class="rounded-[20px] border border-primary px-5 text-primary py-2">Buka peta</a>@endif</div></article>@empty<x-empty-state class="md:col-span-2 lg:col-span-3" title="Lokasi tidak ditemukan" />@endforelse</div><div class="mt-12">{{ $items->links() }}</div></div></section></x-layouts.public>
